---
type Props = {
  label: string;
};

const { label } = Astro.props;
---

<span class="inline-flex rounded-full border border-white/10 bg-white/[0.04] px-2.5 py-1 font-mono text-[0.64rem] uppercase tracking-[0.14em] text-mist">
  {label}
</span>

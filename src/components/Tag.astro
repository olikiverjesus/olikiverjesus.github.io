---
type Props = {
  label: string;
};

const { label } = Astro.props;
---

<span class="inline-flex rounded-full border border-line/80 bg-white/[0.03] px-3 py-1 font-mono text-[0.68rem] uppercase tracking-[0.14em] text-mist">
  {label}
</span>
